export interface SiteInfo {
    siteID: number;
    siteName: string;
    siteGroupName: string;  
    regionName: string; 
    opnameSessionID: number;
    opnameStatus?: string;
    opnameDate?: string;
}